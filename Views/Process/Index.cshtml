@model IEnumerable<beerOfThings.Models.WarmingHistory>;


<a class="bg-transparent hover:bg-green-500 text-yellow-700 font-semibold hover:text-white py-2 px-4 border border-blue-500 hover:border-transparent rounded" href="Process/Create">Waż piwo</a>
<a class="bg-transparent hover:bg-blue-500 text-yellow-700 font-semibold hover:text-white py-2 px-4 border border-blue-500 hover:border-transparent rounded" href="~/Process/Current">Monitoruj proces</a>
<table class="table">
    <thead>
        <tr>
            <th>
                @*@Html.DisplayNameFor(model => model.Name)*@
                Nazwa:
            </th>
            <th>
                Akcje:
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (beerOfThings.Models.WarmingHistory item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Recipe.Name)
                    </td>
                    <td>
                 
                        <a asp-controller = "Recipe" asp-action="Details" asp-route-id="@item.RecipeId">Details</a> |
                        
                    </td>
                </tr>
            }
        </tbody>
</table>


